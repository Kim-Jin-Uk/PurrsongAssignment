import { Button, View } from "react-native"
import defecation from '../datas/defecation.json'
import sleep from '../datas/sleep.json'

const Graph = ({navigation, route}) => {
    defecation.map((v)=>console.log(v))
    return(
        <View>
        </View>
    )
}

export default Graph